{
    "title": "Get insights about repo",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "github_token": {
            "title": "Github token",
            "type": "string",
            "isSecret": true,
            "description": "Github token will be used to get the commits from Github API.",
            "editor": "textfield"
        },
        "prompt": {
            "title": "Prompt for the AI agent",
            "type": "string",
            "description": "Define question or task for the agent.",
            "editor": "textfield",
            "prefill": "Show the most complicated change done last week."
        },
        "repo": {
            "title": "Github repository to be analyzed",
            "type": "string",
            "description": "Github repository that will be used to get commits and answer the question.",
            "editor": "textfield",
            "prefill": "apify/crawlee-python"
        },
        "open_ai_key": {
            "title": "OpenAI token",
            "type": "string",
            "isSecret": true,
            "description": "Your own OpenAI token. Optional, but you will be charged more if you want to use Apify's token.",
            "editor": "textfield"
        }
    },
    "required": ["github_token", "repo", "prompt"]
}
